4章 フィルタリング

条件の評価
where句には1つまたは複数の条件を指定できる。条件はandまたはor演算子で区切って指定する。
where句にand演算子とor演算子で区切られた条件を3つ以上使う場合は()を使て意図を明白にする。
not演算子を使って否定も可能。

条件の種類
大半は等号条件か不等号条件
範囲条件の場合、上限下限を指定する他にbetween演算子を利用することもできる。
